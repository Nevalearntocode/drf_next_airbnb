import React from "react";
import { Button } from "@/components/ui/button";

type Props = {};

const ContactButton = (props: Props) => {
  return <Button className="flex w-full">Contact</Button>;
};

export default ContactButton;
